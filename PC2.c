#include <stdio.h>
#include <stdlib.h>

int main(){
   
    /* Déclarer les variables manquantes */

    printf("Entrer un nombre réel: ");
    multiplie = (int)((n+0.005)*100);
    arrondi = multiplie /100.0;
    printf("%f arrondi à deux chiffres donne: %.2f\n", n, arrondi);

    return 0;
}